# --- Bí mật: tuyệt đối không đẩy lên git ---
.env
.env.*
!.env.example
*.pem
*.key
*credentials*
*secret*

# --- Tài liệu nội bộ: giữ trên máy, không đẩy lên git ---
# Chứa spec thiết kế, đánh giá hệ thống và dữ liệu giá/khoá học thật.
docs/
.tmp-doc-review/

# --- Test: không đẩy lên git (vẫn giữ trên máy để chạy local) ---
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
vitest.config.ts
playwright.config.ts
server/fixtures/
scripts/smoke.ps1
test-results/
playwright-report/
coverage/
__snapshots__/

# Bộ rà soát giao diện bằng Playwright.
# Bỏ dòng này nếu muốn chia sẻ cho cả nhóm dùng chung.
ui-review/

# --- Build & phụ thuộc ---
.next/
node_modules/
dist/
data/
*.log
